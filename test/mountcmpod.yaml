apiVersion: v1
kind: Pod
metadata:
 name: app-pod
spec:
 volumes:
 - name: config-vol
   configMap: 
    name: vol-conf
 containers:
 - name: c1
   image: quay.io/pandeysp/nginxdemo
   volumeMounts:
   - name: config-vol
     mountPath: /data
