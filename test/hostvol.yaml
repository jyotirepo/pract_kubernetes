apiVersion: v1
kind: Pod
metadata:
 name: host-pod
spec:
 volumes:
 - name: host-vol
   hostPath:
    path: /mnt
 containers:
 - name: c1
   image: quay.io/pandeysp/nginxdemo
   volumeMounts:
   - name: host-vol
     mountPath: /data

